---
description: Go backend coding style for SignFlow
globs: ["**/*.go"]
---

# Go 后端代码风格规则

## 代码风格
- 保持代码简洁、直白、工程化
- 优先使用小而清晰的函数
- 命名遵循 Go 常见风格：ID 而不是 Id
- 导出符号只在确实需要跨 package 使用时导出
- 错误要显式返回，不要吞错

## 包职责
- dao 包：只写数据库访问逻辑
- service 包：只写业务编排
- handler 包：只写 HTTP 层逻辑
- model 包：只写结构体定义
- infra/db：只写数据库初始化、健康检查、连接池配置

## 数据库操作
- 使用 GORM
- DAO 中普通读写默认走全局数据库实例
- 事务场景提供 CreateTx / UpdateTx / DeleteTx 等方法
- 涉及 document + workflow + task 联动写入时，必须使用事务

## 错误处理
- 返回明确错误，不要静默失败
- DAO 层可以记录数据库错误日志
- Service 层返回业务错误
- Handler 层负责把错误映射成 HTTP 响应

## 当前优先级
当前优先保证“能跑通业务闭环”，不要过度抽象。
暂时不要主动引入：
- 泛型 DAO
- 复杂中间件体系
- 事件总线
- 过度设计的 DDD 聚合根

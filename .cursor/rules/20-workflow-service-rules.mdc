---
description: Workflow service business rules
globs: ["internal/service/**/*.go"]
---

# Workflow Service 业务规则

## 当前最重要的 service
优先实现：
- DraftWorkflowService.CreateWorkflowDraft（HTTP：`POST /api/v1/workflows`）
- SigningService.Submit

## CreateWorkflowDraft（创建工作流草稿）的业务要求
1. 校验 `fileKey` 对应已上传 PDF、发起人/签署人均为已存在 `UserCode`
2. 创建 DocumentModel（含文件元数据）
3. 创建 WorkflowModel（`draft`）与 WorkflowSigner 列表
4. 使用数据库事务
5. 激活前不产生 Task；激活（`ActivateWorkflow`）后在事务内创建首条 Task 并将 workflow/document 置为可签署状态

输入字段（JSON）：`title`、`description`、`dueDate`、`priority`、`fileKey`、`fileName`、`fileType`、`fileSize`、`initiatorId`、`signers`

输出：`CreateWorkflowDraftResult`（含 `documentId`、`workflowId`、状态与版本等）

## Submit 的业务要求
Submit 要完成：
1. 找到当前 task
2. 校验当前 signer 是否匹配
3. 校验 task 状态是否允许提交
4. 更新 task 为 signed
5. 更新 document version + 1
6. 推进 workflow current_step + 1
7. 创建下一位 signer 的 task
8. 如果没有下一位，则标记 workflow/document completed
9. 全过程使用事务

## 当前业务状态建议
- document.status：draft / ready / signing / completed
- workflow.status：draft / pending / completed / cancelled
- task.status：pending / signed

## 重要原则
- 所有流程推进逻辑都放在 service
- service 可以调用多个 dao
- service 不直接写 HTTP 响应
